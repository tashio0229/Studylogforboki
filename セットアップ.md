---
tags:
  - 設定
---

# ⚙️ GitHub ↔ Obsidian 連携セットアップ

> [!tip] これは何
> GitHubに上がっている勉強記録（努力の証）を、Obsidianに**自動で流し込む**ための一度きりの設定。
> 仕組みは「特定ブランチを自動 `pull` する」だけなので、**既定ブランチを変えなくても連携できる**。

## 大前提（なぜ「連携ボタン」が無いのか）

Obsidianは**GitHubに直接つながらない**。Obsidianが開けるのは「Mac上のフォルダ」だけ。
だから連携＝**Gitがクラウドの中身をMacのフォルダに落とし続ける**こと。その橋渡しを下記で自動化する。

```
GitHub（クラウド・記録の正）  ──自動pull（10分ごと＋起動時）──▶  Macのフォルダ  ──▶  Obsidianが表示
```

## 手順（一度だけ・15分）

### 1. アプリを2つ入れる
- **Obsidian**: https://obsidian.md
- **GitHub Desktop**: https://desktop.github.com （クローンと認証を全部やってくれる。非公開リポジトリでもOK）

### 2. リポジトリをMacに落とす（GitHub Desktop）
1. GitHub Desktop を起動 → GitHubアカウントでサインイン
2. `File → Clone repository` → `tashio0229/Studylogforboki` を選ぶ → 保存先を決めて **Clone**
3. 上部の **`Current Branch`** ドロップダウンで **`claude/fix-conversation-history-BCgiU`** を選ぶ
   （これが「見るブランチ」の指定。以後ここは固定でいい）

### 3. Obsidianでそのフォルダを開く
1. Obsidian起動 → **「フォルダーを保管庫として開く」** → さっきCloneしたフォルダを選ぶ → **「信頼する」**
2. 入口は [[ホーム]]。グラフ・Basesビューは**この保管庫に設定を焼き込み済み**なので、すぐ使える

### 4. 自動連携をONにする（Obsidian Git プラグイン）
1. Obsidian → 設定 → **コミュニティプラグイン** → 「制限モード」をオフ
2. **「閲覧」→ `Git` で検索 → 「Obsidian Git」をインストール → 有効化**
3. これで完了。**設定は committed 済み**（この保管庫内 `.obsidian/plugins/obsidian-git/`）:
   - **起動時＋10分ごとに自動 `pull`**（GitHubの最新が勝手に入る）
   - **`push` は無効**（Macからは送らない＝勉強セッションと絶対にぶつからない・受信専用）

> [!success] これで完成
> あなたが○△✕を秘書（Claudeセッション）に報告 → 記録がGitHubへ → 保管庫が自動再生成 → **MacのObsidianが10分以内に最新化**。
> あなたは何もクリックしなくていい。手動で今すぐ最新化したい時だけ、左のリボンの **Git同期アイコン** か、コマンドパレット「Obsidian Git: Pull」。

## 補足

- **iPhoneでも見たい**: App Storeで「Obsidian」→ iCloud DriveやObsidian Syncで同じフォルダを共有（Obsidian GitはiOSだと制約あり。急がなければMacだけで十分）
- **既定ブランチ変更は不要**: 上の手順2でブランチを直接指定しているため。もし将来「クローン時に何も選ばず最新が欲しい」なら、GitHubのSettings → Default branch を `claude/fix-conversation-history-BCgiU` に変えるとさらに楽（任意）
- **自動再生成（サーバ側）の権限**: GitHub → Settings → Actions → General → Workflow permissions を **Read and write** にすると、ログ更新時にGitHub側で問題ノートが自動再生成される（任意・未設定でも勉強セッションが毎回再生成するので保管庫は最新に保たれる）
