---
updated: "2026-07-27"
status: "方針決定済（2026-07-27）→ 週1回・週末の○流し見ブロック。詳細は 逆算計画-2026-08以降.md"
description: "復習日を過ぎた○245問の現状把握資料。次セッションで消化方針を設計する"
---

# ○バックログ 引き継ぎメモ（2026-07-27 時点）

> ✅ **2026-07-27 夜に方針決定済み。→ [[逆算計画-2026-08以降]] を見ること。**
> 結論：○は放置しても腐らない（エビングハウスの節約率／貯蔵強度）。腐るのは△。
> ただし○復習の目的は防衛でなく**前進**（回さないとチェーンが進まない）。
> よって **平日は△優先のまま／週末に週1回・2〜3hの「○流し見ブロック」を固定枠で置く**。

## 経緯

2026-07-27、本人の指摘で **キュー生成が△✕だけを見ていて○を期限判定から完全に除外していた** ことが判明。
実数は **245問**（最古 6/29＝28日放置）。一度ダッシュボードに「⚪️○流し見ゾーン」として全問投入したが、
本人から次の指示で撤回した。

> 「まさかこんなに溜まっているとは知らなかった。一旦○の運用はまた別で考えよう。
> 今の現状をきちんと俺の方で確認した上で、○の消化については考えていきたい。
> **まずは三角優先であることは変わらない**」

- 撤回した実装は commit `18aa60c`（復元は `e4ba3e9`）。再開時はそこから復元できる＝作り直し不要
- 現在のルール：**○はカウント・報告のみ。キューには入れない**（`.secretary/CLAUDE.md` ①-2 に明記）

## 次セッションでやること

本人の依頼：**「復活したら○の整理のために、現状の把握資料と提案を出してほしい」**

1. 下表を最新化して提示する（集計は下記コマンドで再現できる）
2. 消化方針を **2-3案** 出して比較する（例：週末まとめ流し見／平日△の後に10問ずつ／過去問で間接回収）
3. 本人が選んだ案をダッシュボードとCLAUDE.mdに反映する

## 現状（2026-07-27・○で復習日到来＝245問）

| 放置日数 | 問数 | 主な中身 |
|---|---|---|
| 21日以上 | 148 | 工原 工程別13・個別11・単純総合20・CVP8・部門別7／商業 有形固定資産24・有価証券24・現金預金8・商品売買5・棚卸資産8 |
| 14-20日 | 12 | 工原 単純総合9・意思決定Ⅰ①3 |
| 7-13日 | 38 | 商業 債権債務14／工原 意思決定Ⅰ10・標準4・直接原価3・連産品3 |
| 6日以内 | 47 | 商業 税効果6・外貨3／工原 材料費10・労務費6・意思決定Ⅱ3 |

**内訳の性質**：山の大半は**商会の基礎章（第3-10章＝2級延長の易しい層）**と**工原の総合原価計算系**。
意思決定会計のような重い論点は少ない。「245問」という数字ほど重い塊ではない、というのが所感。

## 再集計コマンド

```bash
python3 - << 'PY'
import re,datetime
from collections import Counter
TODAY=datetime.date.today()
c=Counter(); old={}
for l in open('.secretary/study/exercise-log.md',encoding='utf-8'):
    if not l.startswith('|'): continue
    p=[x.strip() for x in l.split('|')]
    if len(p)<9 or not re.match(r'^[A-Z][A-Z0-9/]*-',p[1]): continue
    try:
        int(p[7]); nd=datetime.date(*map(int,p[6].split('-')))
    except: continue
    if p[4]!='○' or nd>TODAY: continue
    k=p[3]; c[k]+=1
    if k not in old or nd<old[k]: old[k]=nd
for k,v in sorted(c.items(), key=lambda kv:(old[kv[0]],-kv[1])):
    print(f"{k} | {v}問 | 最古{old[k]} | {(TODAY-old[k]).days}日放置")
print("計",sum(c.values()))
PY
```

## 判断材料（提案を作るときの前提）

- ○の間隔ルールは +7 → +14 → +30。本人は 7/17 に「**○は3回転連続○で初めて"真の○"。ただし一度○化していれば復習コストはちょー軽い**」と自ら実証している（○維持のはずの問題が数問△に降格）
- よって「○を放置してよい」ではなく「**軽いコストで回せるはずのものを回せていない**」状態
- ただし本試験までの時間配分では **△✕が先**（本人の原則・⑪の管理会計優先とも整合）
- 245問を1問30〜60秒の流し見で回すと **2〜4時間**。週末1ブロックで一掃できる規模ではある
